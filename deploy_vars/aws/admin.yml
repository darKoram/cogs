
# deploy_vars/aws/admin.yml

# This file should be vaulted to secure users and grouped_users

grouped_users:
  group:
    name: admin
    users:
      - kesten
      - peter
  group: 
     name: dev
     users:
       - dev1
       - dev2
